@model IEnumerable<LeVanTru0714.Models.LVTSinhVien>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LVTTenSinhVien)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LVTTuoi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LVTTenSinhVien)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LVTTuoi)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LVTMaSinhVien">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LVTMaSinhVien">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LVTMaSinhVien">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
